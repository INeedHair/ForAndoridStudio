<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/main_tv_title"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="@string/app_name"
        android:background="@color/lightyellow"
        android:gravity="center"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/pink" />

    <RadioGroup
        android:id="@+id/main_radioButton"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:background="@color/lightyellow"
        android:layout_alignParentBottom="true"
        android:padding="5dp">
        <RadioButton
            android:id="@+id/main_radioButton_star"
            android:drawableTop="@drawable/main_rb_star"
            android:text="@string/label_star"
            style="@style/main_rb"
            android:checked="true"/>
        <RadioButton
            android:id="@+id/main_radioButton_parnter"
            android:drawableTop="@drawable/main_rb_parnter"
            android:text="@string/label_parnter"
            style="@style/main_rb"
            android:checked="false"/>
        <RadioButton
            android:id="@+id/main_radioButton_luck"
            android:drawableTop="@drawable/main_rb_luck"
            android:text="@string/label_luck"
            style="@style/main_rb"
            android:checked="false"/>
        <RadioButton
            android:id="@+id/main_radioButton_me"
            android:drawableTop="@drawable/main_rb_me"
            android:text="@string/label_me"
            style="@style/main_rb"
            android:checked="false"/>
    </RadioGroup>
    <!--中间部分使用布局进行占位，会去使用Fragment替换这块布局-->
    <LinearLayout
        android:id="@+id/main_layout_center"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_below="@id/main_tv_title"
        android:layout_above="@id/main_radioButton"/>
</RelativeLayout>